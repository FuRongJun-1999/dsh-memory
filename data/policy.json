{
  "_comment": "灵枢写入审核策略（合规 + 纪律）。由 md_cg/audit.py:load_rulebook() 经 MDCG_POLICY_FILE 读取。forbidden=命中即 REJECT（禁止写入）；required=缺失即 REJECT（必需要素）。两者全空则 text 类写入恒判 DEFER（'无规则不能假装合规'）。",
  "_note": "本文件只放**高精度**模式——宁可漏报也不误伤正常记忆。长 hex（git SHA）等易误伤的模式刻意不收。",
  "forbidden": [
    "-----BEGIN [A-Z ]*PRIVATE KEY-----",
    "\\bsk-[A-Za-z0-9_\\-]{20,}",
    "\\bAKIA[0-9A-Z]{16}\\b",
    "\\bgh[pousr]_[A-Za-z0-9]{20,}\\b",
    "(?i)\\bapi[_-]?key\\s*[:=]\\s*[A-Za-z0-9_\\-]{16,}",
    "(?i)\\b(app[_-]?secret|appsecret|client[_-]?secret|secret[_-]?key|access[_-]?key)\\s*[:=]?\\s*[A-Za-z0-9]{16,}",
    "(?i)\\b(password|passwd|pwd|密码|口令)\\s*[:=：]\\s*\\S{6,}",
    "(?i)\\bBearer\\s+[A-Za-z0-9._\\-]{20,}",
    "[a-z][a-z0-9+.\\-]*://[^/\\s:@]+:[^/\\s@]+@"
  ],
  "required": []
}
