# 安全审计实锚记录 v0.1

外部审查报告（33 项）的逐项处置实锚。报告原文未随仓库分发，本文档是
**处置状态真源**：每项记「实锚证据 → 裁定 → 载体」。

## 已修复（批次 24，提交 2a57c2a）

| 项 | 处置 | 载体 |
|---|---|---|
| P0-1 node_id 任意文件写入 | 三层纵深：白名单 + realpath 纵深闸 + schema pattern | `md_cg/mdcg.py` `_NODE_ID_RE`、`md_cg/mcp_server.py` |
| P1-1 mdcg_* 绕过 ops_allow | `_MDCG_OP_REQUIRE` 映射 + `_dispatch` 单点闸 + 完备性守卫 | 同上；守卫 `test_security_audit.py` 13 项 |
| P2-1 负记忆死调用双倍 IO | 删除死调用 + 注释改写 | `md_cg/mdcg.py` |
| P1-8 tarfile 解包越界 | `extractall(..., filter="data")` | `scripts/criteria_fingerprint.py` |

字符集裁定（偏离报告建议的理由）：白名单**不含冒号**——NTFS 文件名 `:` 是
备用数据流（ADS）分隔符，`a:b.md` 实际写 `a` 主文件错位，Windows 硬约束；
**含 `@`**——无路径语义且存量在用（`mem_a@exp1` 形态）。

## 本批实锚（批次 25）

### P1-6 read_file 收敛 —— 部分分歧 + 增量已修（批次 26）

实锚：`hive/exec.py` `tool_read_file` 已有 **`HIVE_READ_ROOTS`** 白名单机制
（`read_roots()` + `_under()` 前缀判定，越界即拒），默认放开是
**2026-09-19 使用者裁定**（docstring 留痕）。

**批次 25 裁定勘误**：本文件曾据此判「已闭环」——报告原文到手后确认报告
**明确建议默认收敛到 workdir**，与 2026-09-19 裁定构成真实分歧，不是闭环。
处置（批次 26）：默认放开维持使用者裁定不动；采纳报告建议 3 的增量——
**敏感凭据路径拒读**（`_sensitive_read`：SSH 私钥/云凭据/浏览器 Cookie/
dot-env/密钥扩展，内容回喂外部 LLM 网关前拦截），不受根白名单配置影响。
分歧余项（默认是否收敛）留使用者决策。

### P1-7 自举循环 exec —— 批次 25 实锚错误，批次 26 已修

**批次 25 裁定勘误**：曾把本项错锚到 hive 执行器并判「不适用」——报告原文
确认真实载体是 `scripts/bootstrap_loop.py:228`：`exec(compile(code, ...), ns)`
的 `code` 来自 LLM 返回 / `channel_b_queue.json`（模型可控输入），未沙箱。

处置（批次 26）：`_safe_exec_gen` AST denylist 沙箱——禁 import 面、危险
内建名（eval/exec/compile/open/__import__/反射面）、dunder 属性逃逸链；
命名空间 `__builtins__` 收窄到纯计算内建（双层防御）。自举循环的合法产出
（排序/查找类纯函数）不受影响，冒烟 10 恶意样本全拒。

### P1-2 / P1-3 / P1-4 / P1-5 —— 报告原文到手后批次 26 已修

批次 25 曾判「待报告原文」（记忆载体落空）——使用者提供原文
（`D:\program\dsh-memory-代码审查报告.md`）后对齐施工：

- **P1-2** `src/lib/roleplay_web.ts` `requireEditKey` **fail-closed**：未配置
  `ROLEPLAY_EDIT_KEY` → 写操作一律 403（`editKeyHint()` 引导配置）。此前
  「未配置=开放」的 README 如实化被报告正确指出不够——网页面板是唯一
  浏览器开放面，默认必须关。
- **P1-3** 原型污染：角色 id 白名单 `validRoleId`（`^[A-Za-z0-9_.-]{1,64}$`
  + 显式拒 `__proto__`/`constructor`/`prototype`）+ meta 字典统一 **null
  原型**（`toNullProto`——存量文件已含危险键也只是普通 own 属性）+
  `/meta` 与 `setTranslations` 入口校验。
- **P1-4** 路径根白名单 `check_path_root`（`md_cg/security.py`）：ingest /
  export / link(export/import) / mdcg_ingest 五处接线；
  `MDCG_INGEST_ROOT` / `MDCG_EXPORT_ROOT` **设置即 fail-closed**、未设置
  保持现状（与 HIVE_READ_ROOTS 同构的部署开关语义）；`mdcg_ingest` 的
  权限闸已由批次 24 `_MDCG_OP_REQUIRE`（write）补上。
- **P1-5** `MDCG_LEGACY_ENV_AUTH` 身份不再能自授 admin：需
  `MDCG_LEGACY_ENV_ADMIN=1` **显式二次开关**，只设 `MDCG_CAN_ADMIN` 时
  降级 recorder + stderr 提示（不静默）。

### P0-1 同类残留扫 —— 清零确认

对 `mdcg.py` / `mcp_server.py` / `mdcos.py` 全部 `os.path.join` 面（49 处）
逐点核查：外部可控输入**仅 node_id 一处**（已三层闸）；其余均为内部常量
构造（`_index.json` / `_audit.jsonl` / 分片名 `%06d` 格式化串）或只读探测
（sessions 目录 `os.listdir` 枚举名 + `isdir` 探测，不落盘）。

## 读权限分级（批次 27，使用者需求落地；表述经使用者 2026-09-24 澄清）

**private 的体系语义正名**：private ≠ 个人隐私保密，而是**错误处置标记**——
工作区内容因错误相关、验证未过、待排查而被标记私有，目的是限制向平级/
下级扩散。可见性是**故意的冲突设计**：平级/下级限读，但错误处置链路
（**设计者、上级节点、验证单元**）必须可读。当前令牌矩阵中 verify 等
五单元 `clearance_cap=internal`（forbidden 含 private）与「验证单元必读」
的缺口已于批次 28 按使用者裁定闭环：**verify 单独提 cap 至 private**
（错误处置本职豁免，forbidden 改为仅禁 secret），其余单元维持 internal
限读不变。

**新发现——信封加密的第二层墙（批次 28 实验实锤）**：private/secret 节点
落盘即加密（encrypted_levels），provision_dek 为每个 actor 发独立 DEK、
节点用**写入者 DEK** 加密——跨 actor 读取解密失败静默空（实验：另一
designer 读 seed 的 private 节点亦空）。即 cap 豁免只过密级闸，跨 actor
的 private 实际可读还需 **DEK 共享机制**，三选项待裁定：①DEK 信封共享
（写者 DEK 用读者身份再包信封——改 seal/envelope 结构）；②设计者代查
通道（designer 解密后受控转述）；③private 分型（错误标记 vs 隐私加密
拆开）。不在本批擅自改加密结构。

| 身份 | 文件读面（read_file） | 认知图读面（lingshu_cg read） |
|---|---|---|
| 设计者/主代理 | 全路径（2026-09-19 裁定）+ 部署可选 `HIVE_READ_ROOTS`；文本内容过 **PII 脱敏**（跳过个人敏感信息不入明文） | 其密级上限内全量（designer=secret：public/internal/private） |
| 子代理（worker） | **fail-closed 白名单**：工作区（job_dir）+ 定制工作目录（`spec.workdir`）+ 额外定制目录（`spec.read_roots`，新增）——其他会话/越界拒读；无根即无文件读权限 | 默认密级 **internal**（原 secret 降级）：可读 public/internal，**错误处置标记（private）与 secret 拒读**——平级不消费未证实/已知有误内容 |

实现：`hive/exec.py` `_worker_scope`/`tool_read_file(scope_roots=)`/`_redact_pii`
（邮箱/手机号/身份证/私钥块/API key 样式；正则脱敏是概率防线，新增类别在
`_PII_PATTERNS` 扩展）。编排器派生令牌（`_PRINCIPAL_FACTORY`）身份由调用方
给定，不受默认密级影响。

## 剩余项总览

- **P1 面已全清**（P1-1/2/3/4/5/6增量/7/8）；
- P1-6 余项分歧：读默认是否从「放开」收敛到 workdir——留使用者决策
  （2026-09-19 裁定 vs 报告建议，增量黑名单已先行）；
- P2 剩余 13 项（性能 9 + 安全/健壮性 4，P2-1 已修）：按报告优先序渐进；
- 部署建议：生产设 `HIVE_READ_ROOTS` / `MDCG_INGEST_ROOT` /
  `MDCG_EXPORT_ROOT`（三组根开关的消费侧）。
