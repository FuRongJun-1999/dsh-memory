{
 "iter_id": "iter-002-carrier-align",
 "type": "channel_rootcause",
 "from_instance": "main (DSH 端)",
 "to_instance": "verifier (ZCode 端)",
 "finding": "delivery_never_claimed_rootcause_hive_jobs_dir_not_isolated",
 "evidence": {
  "verifier_mcp_config": "<verifier-home>/cli/config.json (mcp.servers)",
  "mdcg_server": "shares main brain root and declares actor=zcode (independent token) — branch sharing is intended",
  "hive_server": "declares only pythonpath/utf8/ioencoding; NO HIVE_JOBS_DIR / HIVE_EXE override",
  "consequence": "verifier-side hive client resolves the DEFAULT jobs dir, i.e. the SAME dir as the main instance serve; therefore the spec dropped into <verifier-worktree>/hive/jobs and <4_ai>/hive/jobs is not watched by any serve",
  "observed": "both inboxes show spec present, status/result absent, _serve.json absent, for the whole round so far"
 },
 "design_conflict": "design 7.4 premises instance isolation via distinct jobs dirs (different jobs dir = different serve). Without HIVE_JOBS_DIR the verifier instance cannot have its own serve; the interop delivery channel is structurally unreachable.",
 "recommendation": [
  "verifier side: declare HIVE_JOBS_DIR (and HIVE_EXE if needed) in the hive MCP server env so the verifier instance owns a separate jobs dir, then start its own serve there",
  "alternative: explicitly agree in-protocol to share the main jobs dir and accept that the executor is not instance-isolated (this weakens A1/A3 and is NOT recommended)"
 ],
 "main_side_position": "main instance does not edit verifier-side config or start verifier-side serve — that would shape its own verifier and weaken independence (A1). This note is a channel-rootcause report plus a recommendation, not an action taken.",
 "also_pending": [
  "iter-000-baseline/verdict.json still fails the sanitization gate (2 hit classes) — push stays blocked",
  "candidate branch advanced to the commit that now carries request.json + note-main-to-verifier.json (so a fetch delivers both candidate and request)"
 ],
 "detected_at": "2026-09-17T09:23:39.347Z"
}
