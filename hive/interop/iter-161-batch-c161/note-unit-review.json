{
 "kind": "unit-review",
 "iter_id": "iter-161-batch-c161",
 "round": 1,
 "candidate_commit": "cdd267b",
 "branch": "task/iter-161-batch-c161",
 "unit_job": "h1789702708560_aa60c0",
 "verdict": "REJECT",
 "package_n": 13,
 "text": "复核：多数条目与源码分支、早退、返回形状一致；但 `is_protected` 条将 `_fm` 返回 `None` 解释为“节点不在索引且回退无果”，与源码不符：`_fm` 在 `_entry` 为 `None` 时直接 `return None`，不会走 `cg.get` 回退；回退仅发生在条目存在且缺字段时。该误述涉及早退/闭包前置来源，不能全盘接受。\nreason: `is_protected` 对 `_fm` 为 `None` 的前置解释与源码早退不符，应修正后再验。\nverdict: REJECT",
 "paths_policy": "脱敏（模块名已掩码）"
}