{
 "kind": "unit-review",
 "iter_id": "iter-338-batch-c338-fix",
 "round": 2,
 "candidate_commit": "0b98f1a",
 "branch": "task/iter-338-batch-c338-fix",
 "unit_job": "h1789758342203_cc2c0a",
 "verdict": "ACCEPT",
 "package_n": 1,
 "text": "核对：items 由 use_rrf 分支取得；超预算内层条件 `max_item_tokens` 且 `room>=min_excerpt`，`keep=min(max_item_tokens, room)`，`t<=0` 走 `excerpt_empty`，内层 else 走 `oversize_or_over_budget`；截断成功 `t>0` 后实际继续 `used+=t`、append。recent 按 `left` 过滤，返回六键 dict。均在 `recall` 段内，无越界/外源断言。condition 中“否则”需按内层 else 读，与源码不冲突。位置 `position_ok=true`。\nreason: 条件覆盖 input/状态与分支，源码段内可验证，未发现不符或越界。\nverdict: ACCEPT",
 "paths_policy": "脱敏（模块名已掩码）"
}