{
 "kind": "unit-review",
 "iter_id": "iter-254-batch-c254",
 "round": 1,
 "candidate_commit": "c0973dc",
 "branch": "task/iter-254-batch-c254",
 "unit_job": "h1789728485807_555546",
 "verdict": "REJECT",
 "package_n": 43,
 "text": "复核：多数条目吻合，但存在未覆盖的边界/副作用：`_sync_edge_entry` 的 `invalidate_cache` 在 `if entry is not None` 之外，entry 不存在时仍会执行；`_score` 在乘 pools 前先 `min(1.0, ...)`；`verify` 的 confidence 实为 `round(min(0.99,max(0.0,cur+step)),2)`，并非纯 +0.05/-0.15。\nreason: 多个候选条件遗漏源码中的截断、舍入或无条件副作用，不能整体接受。\nverdict: REJECT",
 "paths_policy": "脱敏（模块名已掩码）"
}